port:
  tcp:80:
    listening: false
  tcp:443:
    listening: false
service:
  sshd:
    enabled: false
    running: false
command:
  redis-cli info:
    # required attributes
    exit-status: 0
    # optional attributes
    stdout:
    - redis_version
    stderr: []
    timeout: 500 # in milliseconds
